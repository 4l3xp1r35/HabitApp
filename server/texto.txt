as config que funcionam

DATABASE_URL="postgresql://postgres:postgres@localhost:5001/prog1"

e no compose:
version: '3.1'

services:
  db:
    image: postgres:16
    restart: always
    environment:    
      
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: prog1
      POSTGRES_URL: ${DATABASE_URL}
    ports:
      - 5001:5432
    volumes:
      - ./docker-data/db:/var/lib/postgresql/data